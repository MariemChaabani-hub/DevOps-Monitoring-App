[1mdiff --git a/frontend/src/components/ServerList.js b/frontend/src/components/ServerList.js[m
[1mindex 5343834..40bea05 100644[m
[1m--- a/frontend/src/components/ServerList.js[m
[1m+++ b/frontend/src/components/ServerList.js[m
[36m@@ -6,7 +6,25 @@[m
 import React from 'react';[m
 import './ServerList.css';[m
 [m
[32m+[m[32m// Compares two "x.y.z" version strings numerically (not lexically — "1.10.0"[m
[32m+[m[32m// must beat "1.9.0"). Missing/malformed versions sort lowest.[m
[32m+[m[32mconst compareVersions = (a, b) => {[m
[32m+[m[32m  const partsA = (a || '0').split('.').map(n => parseInt(n, 10) || 0);[m
[32m+[m[32m  const partsB = (b || '0').split('.').map(n => parseInt(n, 10) || 0);[m
[32m+[m[32m  for (let i = 0; i < Math.max(partsA.length, partsB.length); i++) {[m
[32m+[m[32m    const diff = (partsA[i] || 0) - (partsB[i] || 0);[m
[32m+[m[32m    if (diff !== 0) return diff;[m
[32m+[m[32m  }[m
[32m+[m[32m  return 0;[m
[32m+[m[32m};[m
[32m+[m
 const ServerList = ({ servers, selectedServer, onSelectServer, alerts = [] }) => {[m
[32m+[m[32m  // Newest agent_version observed across all servers — used to flag a[m
[32m+[m[32m  // server running stale agent code (e.g. missing detection features)[m
[32m+[m[32m  // instead of that looking like a bug in the app itself.[m
[32m+[m[32m  const latestAgentVersion = servers.reduce((latest, s) => ([m
[32m+[m[32m    s.agent_version && compareVersions(s.agent_version, latest) > 0 ? s.agent_version : latest[m
[32m+[m[32m  ), null);[m
   /**[m
    * Calculate server status based on its active alerts[m
    * CRITICAL > WARNING > OK[m
[36m@@ -88,6 +106,24 @@[m [mconst ServerList = ({ servers, selectedServer, onSelectServer, alerts = [] }) =>[m
                   <span className="label">Emplacement :</span>[m
                   <span className="value">{server.location}</span>[m
                 </div>[m
[32m+[m[32m                {server.agent_version && ([m
[32m+[m[32m                  <div className="detail-item">[m
[32m+[m[32m                    <span className="label">Agent :</span>[m
[32m+[m[32m                    <span[m
[32m+[m[32m                      className="value"[m
[32m+[m[32m                      title={[m
[32m+[m[32m                        latestAgentVersion && compareVersions(server.agent_version, latestAgentVersion) < 0[m
[32m+[m[32m                          ? `Agent obsolète — dernière version observée : v${latestAgentVersion}`[m
[32m+[m[32m                          : ''[m
[32m+[m[32m                      }[m
[32m+[m[32m                    >[m
[32m+[m[32m                      v{server.agent_version}[m
[32m+[m[32m                      {latestAgentVersion && compareVersions(server.agent_version, latestAgentVersion) < 0 && ([m
[32m+[m[32m                        <span className="agent-version-outdated"> ⚠ obsolète</span>[m
[32m+[m[32m                      )}[m
[32m+[m[32m                    </span>[m
[32m+[m[32m                  </div>[m
[32m+[m[32m                )}[m
               </div>[m
 [m
               {/* Alert Summary */}[m
[36m@@ -161,7 +197,8 @@[m [mconst ServerList = ({ servers, selectedServer, onSelectServer, alerts = [] }) =>[m
               </div>[m
             )}[m
           </div>[m
[31m-        ))}[m
[32m+[m[32m          );[m
[32m+[m[32m        })}[m
 [m
         {servers.length === 0 && ([m
           <div className="no-servers">[m
