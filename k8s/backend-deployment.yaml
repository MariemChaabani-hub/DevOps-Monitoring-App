apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
<<<<<<< HEAD
        image: mariemchaabani/monitoring-backend:27
=======
        image: mariemchaabani/monitoring-backend:33
>>>>>>> 69f6490627bff613d391556f18a5b5d6323f2686
        ports:
        - containerPort: 3000
        envFrom:
        - secretRef:
            name: monitoring-secret
        volumeMounts:
        - name: docker-sock
          mountPath: /var/run/docker.sock
        - name: backups
          mountPath: /app/backups
      volumes:
      - name: docker-sock
        hostPath:
          path: /var/run/docker.sock
      - name: backups
        hostPath:
          path: /home/debian/DevOps-Monitoring-App/backups
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: monitoring
spec:
  selector:
    app: backend
  ports:
  - port: 3000
    targetPort: 3000
  nodePort: 30300
  type: NodePort
